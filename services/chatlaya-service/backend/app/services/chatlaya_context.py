from __future__ import annotations


async def build_chatlaya_product_context(current: dict | None, guest_id: str | None) -> str:
    # TODO(chatlaya-service): replace with core API client calls after extraction.
    _ = (current, guest_id)
    return ""
